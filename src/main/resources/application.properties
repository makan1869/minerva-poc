# Server configuration
server.port=8080

# Application name
spring.application.name=minerva-poc

# Logging configuration
logging.level.root=INFO
logging.level.com.example.minerva=DEBUG

# AI-MC-Server configuration
ai-mc-server.enabled=true
ai-mc-server.endpoint=/ai-mc
